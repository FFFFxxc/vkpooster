{
  "name": "vk-reposter-pro",
  "version": "4.0.0",
  "description": "VK Reposter Pro — расширение для репоста в ВКонтакте",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js && npm --prefix server test",
    "check:js": "node --check safety-core.js && node --check background.js && node --check popup-auth.js && node --check content.js && node --check scheduled.js && npm --prefix server run check",
    "verify": "npm test && npm run check:js"
  }
}
